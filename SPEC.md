# HIMRA v5.0 Specification — "Human Memory"

**Version:** 5.0.0
**Branch:** v5-human-memory
**Date:** 2026-06-19
**Author:** Zhang Jing

---

## 1. 概述

HIMRA v5.0 在 v4.1 的检索架构之上，新增了一个完整的平行子系统——**记忆生命周期管理**。v1-v4 解决的是"怎么找到记忆"（检索维度），v5 新增的是"记忆怎么生长和衰减"（生命周期维度）。

**双轴架构：**
- **检索轴**（继承 v4.1）：规则匹配 → 实体匹配 → 语义检索 → 图遍历
- **生命周期轴**（v5 新增）：短期记忆 → 巩固 → 长期记忆 → 衰减

## 2. 核心概念

### 2.1 记忆层级

| 层级 | 目录 | 用途 | 生命周期 |
|------|------|------|----------|
| 短期记忆 | `memory/short-term/` | 新接收的信息，未经验证 | 14天未被召回则归档 |
| 长期记忆 | `memory/long-term/` | 被验证有用的信息 | 持久保留，定期更新 |
| 归档 | `memory/summaries/` | 衰减后的记忆 | 只读，可被重新激活 |

### 2.2 巩固机制

**核心原则：** 不做复杂的自动统计埋点，利用 Hindsight 自带的召回能力判断"被反复使用"。

**巩固条件：**
- 同一条短期记忆被 Hindsight 在不同会话中召回 ≥ 3 次 → 自动迁移到长期记忆
- 超过 14 天未被 Hindsight 召回 → 移入 summaries/ 归档
- 用户手动标记"记住这个" → 直接写入长期记忆（`manual_override: true`）

### 2.3 与 Hindsight 的分工

| 职责 | HIMRA | Hindsight |
|------|-------|-----------|
| 存储 | 管"记忆放哪个文件夹、属于哪个类别" | 不管存储结构 |
| 检索 | 管"什么情况下去搜、搜什么" | 管"怎么搜"——语义匹配、相似度排序 |
| 生命周期 | 管"什么条件从短期升长期、什么条件衰减" | 管"统计这条记忆被召回了几次" |

**一句话：** HIMRA 是大脑结构（新皮层分区），Hindsight 是搜索引擎（海马体的模式完成功能）。

### 2.4 retrieval_count 更新机制

**方案 A：由 Hermes 负责更新，不依赖 Hindsight 改 API。**

当 Hermes 在检索流程中使用了一条短期记忆（无论是走 HIMRA 路由命中还是 Hindsight 语义召回），必须执行以下回写：

1. 打开对应的 `short-term/YYYY-MM-DD/session-XXXX.md`
2. 将 `retrieval_count` 字段值 +1
3. 将 `last_retrieved` 字段更新为当前时间戳
4. 同步更新 `short-term/index.md` 中对应行的召回次数和最后召回时间

**理由：** Hindsight API 目前不暴露检索统计字段，与其等它支持，不如让 Hermes 在检索时就地更新。这符合 HIMRA v5.0 的分工原则——HIMRA 管生命周期，Hindsight 只管语义匹配。
